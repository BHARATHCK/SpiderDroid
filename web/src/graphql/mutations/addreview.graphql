mutation AddReview(
  $addReviewCommentText: String!
  $addReviewBookingId: Float!
  $addReviewCommentTitle: String!
) {
  addReview(
    commentText: $addReviewCommentText
    bookingId: $addReviewBookingId
    commentTitle: $addReviewCommentTitle
  )
}
