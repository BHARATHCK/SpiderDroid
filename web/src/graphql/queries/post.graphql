query Post($postId: Float!) {
  post(id: $postId) {
    id
    carMake
    carModel
    imageUrl
    carYear
    trips
    points
    destination {
      destinationName
    }
    creator {
      username
    }
    carCostPerDay
  }
}
