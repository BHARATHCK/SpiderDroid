query Profile {
  me {
    ...RegularUser
    bookings {
      id
      carId
      fromDate
      toDate
      ratingStatus
      bookingStatus
    }
    posts {
      id
      carMake
      carModel
      carYear
    }
  }
}
